# =======================================================================
# Squid Strict Egress Proxy Configuration
# =======================================================================

# Port to listen on
http_port 3128

# ACLs
acl localnet src 10.0.0.0/8
acl localnet src 172.16.0.0/12
acl localnet src 192.168.0.0/16
acl localnet src fc00::/7
acl localnet src fe80::/10

acl SSL_ports port 443
acl Safe_ports port 80      # http
acl Safe_ports port 443     # https
acl CONNECT method CONNECT

# -----------------------------------------------------------------------
# WHITELISTED DOMAINS
# Add any required API domains here.
# -----------------------------------------------------------------------
acl whitelisted_domains dstdomain .openai.com
acl whitelisted_domains dstdomain .anthropic.com
acl whitelisted_domains dstdomain .googleapis.com
acl whitelisted_domains dstdomain .google.com
# For typical pip installs if needed during build, but we shouldn't proxy build traffic unless necessary.
# If backend needs any other external APIs, add them here.

# -----------------------------------------------------------------------
# ACCESS RULES
# -----------------------------------------------------------------------
# Deny requests to certain unsafe ports
http_access deny !Safe_ports

# Deny CONNECT to other than secure SSL ports
http_access deny CONNECT !SSL_ports

# Only allow cachemgr access from localhost
http_access allow localhost manager
http_access deny manager

# ALLOW TRAFFIC TO WHITELISTED DOMAINS ONLY
http_access allow localnet whitelisted_domains
http_access allow localhost whitelisted_domains

# AND FINALLY DENY ALL OTHER ACCESS TO THIS PROXY
http_access deny all

# No caching for a secure API proxy
cache deny all
